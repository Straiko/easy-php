# 📤 Публикация проекта на GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com
2. Войдите в свой аккаунт (или создайте новый)
3. Нажмите кнопку **"+"** в правом верхнем углу
4. Выберите **"New repository"**
5. Заполните форму:
   - **Repository name**: `php-project` (или любое другое имя)
   - **Description**: "PHP project with automatic PHP installer for Windows"
   - Выберите **Public** (или Private, если хотите)
   - **НЕ** ставьте галочки на "Initialize with README", "Add .gitignore", "Choose a license"
     (у нас уже есть эти файлы)
6. Нажмите **"Create repository"**

## Шаг 2: Подключите локальный репозиторий к GitHub

После создания репозитория GitHub покажет инструкции. Выполните команды:

```bash
cd C:\Users\starikov\php-project

# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/php-project.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

## Шаг 3: Проверьте результат

Откройте ваш репозиторий на GitHub:
```
https://github.com/YOUR_USERNAME/php-project
```

Вы должны увидеть все файлы проекта!

## 🔐 Если нужна аутентификация

### Вариант 1: Personal Access Token (рекомендуется)

1. GitHub Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите права: `repo` (полный доступ к репозиториям)
4. Скопируйте токен
5. При `git push` используйте токен вместо пароля:
   ```
   Username: ваш_username
   Password: ваш_токен
   ```

### Вариант 2: GitHub CLI

```bash
# Установите GitHub CLI
winget install GitHub.cli

# Авторизуйтесь
gh auth login

# Создайте репозиторий и опубликуйте
gh repo create php-project --public --source=. --remote=origin --push
```

## 📝 Дополнительные команды Git

```bash
# Проверить статус
git status

# Добавить изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push

# Получить обновления
git pull
```

## ✅ Готово!

Ваш проект теперь на GitHub! Другие могут:
- Клонировать проект: `git clone https://github.com/YOUR_USERNAME/php-project.git`
- Смотреть код
- Создавать Issues
- Делать Pull Requests

## 🎯 Следующие шаги

1. Добавьте описание проекта в README.md
2. Добавьте теги/топики на странице репозитория
3. Создайте Releases для версий проекта
4. Добавьте badges в README (статус сборки, версия и т.д.)

